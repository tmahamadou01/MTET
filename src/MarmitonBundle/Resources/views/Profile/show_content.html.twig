{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    <p>{{ 'profile.show.username'|trans }} BEKLE : {{ user.username }}</p>
    <p>{{ 'profile.show.email'|trans }} BEKE : {{ user.email }}</p>
</div>
